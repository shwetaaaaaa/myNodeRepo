#--------------------------------------
# change the `host` property to a
# unique hostname on the domain you
# are deploying to.
#--------------------------------------

applications:
- name:    myNodeRepo
  host:    myFirstNodeApplication
  memory:  128M
  command: node server.js
